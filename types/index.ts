export type Event = {
  id: string;
  title: string;
  date: string;
  location: string;
  category: string;
  image: string;
  description: string;
};

export type Organization = {
  id: string;
  name: string;
  category: string;
};